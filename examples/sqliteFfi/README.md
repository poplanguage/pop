# SQLite FFI Pop binary

This ordinary Pop Package links the host SQLite library through `Pop.Ffi`; it
does not require a preprogrammed native shim.

```text
pop run --manifestPath examples/sqliteFfi/bubble.toml
```
