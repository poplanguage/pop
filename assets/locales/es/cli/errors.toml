[messages."cli.missingCommand"]
text = "falta el comando"

[messages."cli.unsupportedCommand"]
text = "comando no compatible `{command}`"

[messages."cli.unsupportedLanguage"]
text = "idioma no compatible `{language}`; se esperaba en, zh-Hans, ja, pt-BR o es"

[messages."cli.languageNeedsValue"]
text = "`--language` requiere una etiqueta de idioma"

[messages."cli.selectLanguageFailed"]
text = "no se pudo seleccionar el idioma de la herramienta: {detail}"

[messages."cli.checkNeedsSource"]
text = "`pop check` requiere una ruta de fuente .pop o `--manifestPath`"

[messages."cli.commandRequiresOption"]
text = "`pop {command}` requiere `{option}`"

[messages."cli.unsupportedOption"]
text = "opción no compatible `{option}`"

[messages."cli.expectedOption"]
text = "opción no compatible `{option}`; se esperaba `{expected}`"

[messages."cli.optionRequiresValue"]
text = "`{option}` requiere {value}"

[messages."cli.unexpectedArguments"]
text = "`pop {command}` recibió argumentos inesperados"

[messages."cli.oneSource"]
text = "`pop {command}` acepta exactamente una ruta de fuente en el modo bootstrap"

[messages."cli.sourceRequired"]
text = "`pop {command}` requiere una ruta de fuente .pop"

[messages."cli.manifestRequired"]
text = "`pop {command} --manifestPath` requiere una ruta bubble.toml"

[messages."cli.manifestName"]
text = "`pop {command} --manifestPath` requiere una ruta llamada bubble.toml"

[messages."cli.unsupportedChoice"]
text = "{choice} no compatible `{value}`; se esperaba {expected}"

[messages."cli.unsupportedRuntimeProfile"]
text = "perfil de runtime no compatible `{value}`"

[messages."cli.manifestOption"]
text = "opción de manifiesto no compatible `{option}`; se esperaba --locked, --offline o --frozen"

[messages."cli.programArgumentsSeparator"]
text = "opción no compatible `{option}`; los argumentos del programa deben ir después de `--`"

[messages."cli.bpfRequires"]
text = "las compilaciones BPF requieren `{option}`"

[messages."cli.readFailed"]
text = "no se pudo leer `{path}`: {detail}"

[messages."cli.loadFailed"]
text = "no se pudo cargar `{path}`: {detail}"

[messages."cli.writeHelpFailed"]
text = "no se pudo escribir la ayuda: {detail}"

[messages."cli.writeDiagnosticsFailed"]
text = "no se pudieron escribir los diagnósticos: {detail}"

[messages."cli.diagnosticLimitReached"]
text = "se detuvo después de {maximum} errores; se omitieron {omitted} errores adicionales"

[messages."cli.renderDiagnosticsFailed"]
text = "no se pudieron renderizar los diagnósticos: {detail}"

[messages."cli.writeOutputFailed"]
text = "no se pudo escribir la salida: {detail}"

[messages."cli.internalCompilerError"]
text = "error interno del compilador: {detail}"

[messages."cli.externalFailure"]
text = "{context}: {detail}"

[messages."cli.toolFailure"]
text = "la operación de la herramienta falló: {detail}"

[messages."cli.transpileNeedsFormat"]
text = "`--to` requiere un formato de código fuente de backend (`c`)"

[messages."cli.unsupportedTranspileTarget"]
text = "destino de transpilación `{value}` no compatible; se esperaba `c`"

[messages."cli.buildNeedsOutputOrTarget"]
text = "`pop build` requiere una ruta de salida (`--output`) o una terna de destino (`--target`)"

[messages."cli.targetNeedsTriple"]
text = "`--target` requiere una terna de destino"

[messages."cli.runtimeProfileNeedsName"]
text = "`--runtime-profile` requiere un nombre de perfil"

[messages."cli.unsupportedBpfProgram"]
text = "programa BPF `{value}` no compatible; se esperaba `xdp`"

[messages."cli.emitObjectNeedsPath"]
text = "`--emit-object` requiere una ruta de archivo objeto"

[messages."cli.outputNeedsExecutablePath"]
text = "`--output` requiere una ruta de ejecutable"

[messages."cli.runNeedsInput"]
text = "`pop run` requiere una ruta de fuente .pop o `--manifestPath`"

[messages."cli.unsupportedDumpKind"]
text = "tipo de volcado `{value}` no compatible; se esperaba `hir`, `mir` o `ll`"

[messages."cli.newNeedsPath"]
text = "`pop new` requiere una ruta de destino"

[messages."cli.scaffoldKindConflict"]
text = "seleccione solo `--library` o `--binary`"

[messages."cli.scaffoldFailed"]
text = "no se pudo crear el Package: {detail}"

[messages."cli.packageCreated"]
text = "se creó el Package `{name}` en `{path}`"
