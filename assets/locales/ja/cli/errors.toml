[messages."cli.missingCommand"]
text = "コマンドがありません"

[messages."cli.unsupportedCommand"]
text = "未対応のコマンド `{command}`"

[messages."cli.unsupportedLanguage"]
text = "未対応の言語 `{language}`。en、zh-Hans、ja、pt-BR、es のいずれかを指定してください"

[messages."cli.languageNeedsValue"]
text = "`--language` には言語タグが必要です"

[messages."cli.selectLanguageFailed"]
text = "ツールの言語を選択できませんでした: {detail}"

[messages."cli.checkNeedsSource"]
text = "`pop check` には .pop ソースパスまたは `--manifestPath` が必要です"

[messages."cli.commandRequiresOption"]
text = "`pop {command}` には `{option}` が必要です"

[messages."cli.unsupportedOption"]
text = "未対応のオプション `{option}`"

[messages."cli.expectedOption"]
text = "未対応のオプション `{option}`。`{expected}` が必要です"

[messages."cli.optionRequiresValue"]
text = "`{option}` には {value} が必要です"

[messages."cli.unexpectedArguments"]
text = "`pop {command}` に予期しない引数が渡されました"

[messages."cli.oneSource"]
text = "ブートストラップモードの `pop {command}` はソースパスを 1 つだけ受け取ります"

[messages."cli.sourceRequired"]
text = "`pop {command}` には .pop ソースパスが必要です"

[messages."cli.manifestRequired"]
text = "`pop {command} --manifestPath` には bubble.toml パスが必要です"

[messages."cli.manifestName"]
text = "`pop {command} --manifestPath` には bubble.toml という名前のパスが必要です"

[messages."cli.unsupportedChoice"]
text = "未対応の {choice} `{value}`。{expected} が必要です"

[messages."cli.unsupportedRuntimeProfile"]
text = "未対応の runtime プロファイル `{value}`"

[messages."cli.manifestOption"]
text = "未対応の manifest オプション `{option}`。--locked、--offline、--frozen のいずれかが必要です"

[messages."cli.programArgumentsSeparator"]
text = "未対応のオプション `{option}`。プログラム引数は `--` の後に指定してください"

[messages."cli.bpfRequires"]
text = "BPF build には `{option}` が必要です"

[messages."cli.readFailed"]
text = "`{path}` を読み取れませんでした: {detail}"

[messages."cli.loadFailed"]
text = "`{path}` を読み込めませんでした: {detail}"

[messages."cli.writeHelpFailed"]
text = "ヘルプを書き込めませんでした: {detail}"

[messages."cli.writeDiagnosticsFailed"]
text = "診断を書き込めませんでした: {detail}"

[messages."cli.diagnosticLimitReached"]
text = "{maximum} 件のエラーで停止しました。追加の {omitted} 件のエラーは省略されました"

[messages."cli.renderDiagnosticsFailed"]
text = "診断を表示できませんでした: {detail}"

[messages."cli.writeOutputFailed"]
text = "出力を書き込めませんでした: {detail}"

[messages."cli.internalCompilerError"]
text = "コンパイラ内部エラー: {detail}"

[messages."cli.externalFailure"]
text = "{context}: {detail}"

[messages."cli.toolFailure"]
text = "ツールの操作に失敗しました: {detail}"

[messages."cli.transpileNeedsFormat"]
text = "`--to` にはバックエンドのソース形式（`c`）が必要です"

[messages."cli.unsupportedTranspileTarget"]
text = "未対応のトランスパイル対象 `{value}` です。`c` を指定してください"

[messages."cli.buildNeedsOutputOrTarget"]
text = "`pop build` には出力パス（`--output`）またはターゲットトリプル（`--target`）が必要です"

[messages."cli.targetNeedsTriple"]
text = "`--target` にはターゲットトリプルが必要です"

[messages."cli.runtimeProfileNeedsName"]
text = "`--runtime-profile` にはプロファイル名が必要です"

[messages."cli.unsupportedBpfProgram"]
text = "未対応の BPF プログラム `{value}` です。`xdp` を指定してください"

[messages."cli.emitObjectNeedsPath"]
text = "`--emit-object` にはオブジェクトファイルのパスが必要です"

[messages."cli.outputNeedsExecutablePath"]
text = "`--output` には実行可能ファイルのパスが必要です"

[messages."cli.runNeedsInput"]
text = "`pop run` には .pop ソースパスまたは `--manifestPath` が必要です"

[messages."cli.unsupportedDumpKind"]
text = "未対応のダンプ種別 `{value}` です。`hir`、`mir`、`ll` のいずれかを指定してください"

[messages."cli.newNeedsPath"]
text = "`pop new` には出力先パスが必要です"

[messages."cli.scaffoldKindConflict"]
text = "`--library` または `--binary` の一方だけを選択してください"

[messages."cli.scaffoldFailed"]
text = "Package を作成できませんでした: {detail}"

[messages."cli.packageCreated"]
text = "Package `{name}` を `{path}` に作成しました"
